{
  "name": "fieldwindow",
  "version": "0.2.0",
  "private": true,
  "description": "Explainable weather-window planning for drone and outdoor field operations",
  "scripts": {
    "test": "node --test",
    "check": "node --check api/mcp.js && node --check api/health.js"
  },
  "engines": { "node": ">=20" }
}
